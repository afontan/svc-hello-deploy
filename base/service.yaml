apiVersion: v1
kind: Service
metadata:
  name: svc-hello
  labels:
    app.kubernetes.io/name: svc-hello
spec:
  selector:
    app.kubernetes.io/name: svc-hello
  ports:
    - name: http
      port: 80
      targetPort: 8080
